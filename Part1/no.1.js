//no.1 List all the primitive data types in JavaScript and give a brief explanation of each.
//  below the primitive datatypes
// strings. This data type stores text and is written in quotes
// Example:
let text = "Juliet ken";
console.log(text);

// number. A nummber can be with or with out decimal. An extra large number can be written with an exponention.
// Example:
let x = 30    // number with no decimal
let y = 30.5    // number with decimal
console.log(x,y);
// Booleans. A boolean represents one of two values: yes or no, true or false.
// we can use a boolean to find out if an expression is true.
// Example.
Boolean = (15>5)
Boolean1 = (8<5)
console.log(Boolean);
console.log(Boolean1)

// BigInt. these are used to store big interger values that can not be represented by a normal javascript number.
// Example.
let M = 88888888888888889
let N = 99999999999999n
console.log(M,N);

//null
// object.objects are variables but can contain many values.
// example of object.
const car = {type:"BMW", model:"700", color:"white"};
console.log(car);
let car2 = "rangerover"
console.log(car2);

// undefined. these are never with values.
// example.
let X;

// symbol.

